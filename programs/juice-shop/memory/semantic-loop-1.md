This loop opened the authorized CASE path against the local Juice Shop lab only.

Defense: stock docker (v0) is no longer the wall; v1-hardened sits in front with security headers, login rate limit, and the extra-file path closed.

UX: fail-closed scope file, recon skipped on loopback, Flow Studio CASE graph gained an in-scope recon node plus a score pill, and `lanbb case report` still writes a draft at 0/N.

Score 0/116 is honest — CASE tools did not auto-pwn. The product metric this loop is that the report and memory emit still run when the wall is higher.

Procedural memory: three SKILL.md files (parse-scope, passive-recon, write-report). No fourth skill. Working memory was not written.
