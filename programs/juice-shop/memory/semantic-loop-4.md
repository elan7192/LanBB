Loop 4 hunted the v3-hardened wall, not v2 and not stock. GET /api/Challenges/ did not arrive (connection refused; docker not installed). Honest score 0/116. CASE tools did not auto-pwn and did not invent n.

Defense: v4-hardened is now the wall. It keeps the v3 pin, method allowlist, URI WAF, cookie/COEP/HSTS, read-only edge, and closed upload/PII/chatbot/B2B/snippets surfaces, then raises app/edge caps, broader URI WAF tokens, tighter body/rate/connection limits, and closes GraphQL, basket/commerce, reviews/feedback, captcha, and remaining data-export surfaces v3 left open.

UX: Flow Studio now shows fill provenance next to n/N — a hunted pill and a fill pill (live vs unavailable) so a 0/N fill still names the overlay that was hunted versus the current wall. Catalog rows carry hunted and fill. Harden-node inspector lists the next wall. versions.json records hunted=v3-hardened, wall=v4-hardened, fill=unavailable 0/116.

Coverage honesty: the 13-skill pack still does not cover Cryptographic Issues, Miscellaneous, Security Misconfiguration, or Security through Obscurity. Docker-off: Insecure Deserialization and XXE were not exercised. Coding /snippets stay out of n/N.

Product metric this loop is the same: report and memory emit still run when the wall is higher and the score is 0/N with no live Fill. Next hunt must use v4, not v3.
