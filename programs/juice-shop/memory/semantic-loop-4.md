Loop 4 hunted the v3-hardened wall, not v2 and not stock. Fill on the box after that overlay scored live 0/116 (GET /api/Challenges/, docker_disabled=18). CASE tools did not auto-pwn, did not forge continue-code, and did not rediscover.

Defense: v4-hardened is now the wall. It keeps the v3 pin, method allowlist, URI WAF, cookie/COEP/HSTS, read-only edge, and closed upload/PII/chatbot/B2B/snippets surfaces, then raises juice process caps, origin/UA deny, CORS strip, stronger WAF signatures, and closes registration, search, reviews/feedback, baskets, captcha, and privacy surfaces v3 left open.

UX: Flow Studio now shows fill live vs unknown — a fill pill next to n/N and wall, catalog rows carry fill, and the lab node inspector lists fill plus docker_disabled. versions.json records hunted=v3-hardened, wall=v4-hardened, fill=live 0/116.

Coverage honesty: the 13-skill pack still does not cover Cryptographic Issues, Miscellaneous, Security Misconfiguration, or Security through Obscurity. Docker-off: Insecure Deserialization and XXE were not exercised. Coding /snippets stay out of n/N.

Product metric this loop is the same: report and memory emit still run when the wall is higher and the live Fill score is 0/N. Next hunt must use v4, not v3.
