Loop 6 hunted the v5-hardened wall, not v4 and not stock. Fill on this box after that overlay was unavailable (GET /api/Challenges/ connection refused; docker not installed). Honest score 0/116. CASE tools did not auto-pwn and did not rediscover.

Defense: v6-hardened is now the wall. It keeps the v5 pin, juice read-only plus tmpfs, dropped OPTIONS, identity/Web3/catalog/search/info-leak closures, and nginx limit_req burst>=1, then closes the remaining auth door (/rest/user/login and the rest of /rest/user), drops POST so only GET/HEAD remain, broadens WAF-ish signatures, tightens mem/pids/cpu and tmpfs size caps, adds CSP trusted-types default, and closes the SPA/static leak class v5 left open (assets, i18n, source maps, sitemap, leftover frontend routes).

UX: Flow Studio now names coding /snippets as out of n/N. A coding pill sits beside n/N, fill, hunted, and next-hunt so a 0/N score is not mistaken for coding challenges. Catalog rows carry coding_challenges and docker_disabled_env. Lab and harden inspectors list Coding challenges. versions.json records hunted=v5-hardened, wall=v6-hardened, fill=unavailable 0/116.

Coverage honesty: the 13-skill pack still does not cover Cryptographic Issues, Miscellaneous, Security Misconfiguration, or Security through Obscurity. Docker-off: Insecure Deserialization and XXE were not exercised. Coding /snippets stay out of n/N.

Product metric this loop is the same: report and memory emit still run when the wall is higher and Fill is unavailable 0/N. Next hunt must use v6, not v5.
