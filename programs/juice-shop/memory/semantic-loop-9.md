Loop 9 hunted the v8-hardened wall, not v7 and not stock. Fill on this box was unavailable (GET /api/Challenges/ connection refused; docker not installed). Honest score 0/116. Last live Fill remains 0/116 on v7 APPLIES (GET 200, default-deny 403, POST 405). Do not invent n. CASE tools did not auto-pwn.

Defense: v9-hardened keeps the working-harden apply constraints (no juice EROFS, no tmpfs over data/static, nginx limit_req burst>=1, GET /api/Challenges/ stays open for n/N, juice 128m). It raises the wall with exact-equals score locations (not the v8 regex), host allowlist 127.0.0.1|localhost, inbound cookies stripped, leftover oauth/logout/health/debug/config closed, broader static deny, tighter edge caps, and extra headers/WAF signatures.

UX: Flow Studio keeps last-live GET 200 / 403 / POST 405 on last-live fields when this hunt's fill is unavailable, so 0/N is not mistaken for a missed wall or a lost v7 APPLIES proof. Catalog, inspector, score API, and the report draft all name this-hunt fill vs last-live evidence.

Coverage honesty: the 13-skill pack still does not cover Cryptographic Issues, Miscellaneous, Security Misconfiguration, or Security through Obscurity. Docker-off: Insecure Deserialization and XXE were not exercised. Coding /snippets stay out of n/N.

Product metric this loop is the same: report and memory emit still run when the wall is higher and this hunt's Fill is unavailable at 0/N. Next hunt must use v9, not v8.
